diff --git a/vllm/entrypoints/openai/api_server.py b/vllm/entrypoints/openai/api_server.py
index 95fcd0cd8..4b6a3937d 100644
--- a/vllm/entrypoints/openai/api_server.py
+++ b/vllm/entrypoints/openai/api_server.py
@@ -5,6 +5,7 @@ import atexit
 import gc
 import importlib
 import inspect
+import json
 import multiprocessing
 import os
 import signal
@@ -1114,6 +1115,16 @@ if envs.VLLM_ALLOW_RUNTIME_LORA_UPDATING:
 
         return Response(status_code=200, content=response)
 
+def load_log_config(log_config_file: Optional[str]) -> Optional[dict]:
+    if not log_config_file:
+        return None
+    try:
+        with open(log_config_file) as f:
+            return json.load(f)
+    except Exception as e:
+        logger.warning("Failed to load log config from file %s: error %s",
+                       log_config_file, e)
+        return None
 
 def build_app(args: Namespace) -> FastAPI:
     if args.disable_fastapi_docs:
@@ -1417,7 +1428,10 @@ async def run_server(args, **uvicorn_kwargs) -> None:
         logger.info("Starting vLLM API server on http%s://%s:%d",
                     "s" if is_ssl else "", _listen_addr(sock_addr[0]),
                     sock_addr[1])
-
+        # Load logging config for uvicorn if specified
+        log_config = load_log_config(args.log_config_file)
+        if log_config is not None:
+            uvicorn_kwargs['log_config'] = log_config
         shutdown_task = await serve_http(
             app,
             sock=sock,
diff --git a/vllm/entrypoints/openai/cli_args.py b/vllm/entrypoints/openai/cli_args.py
index d01af5e42..0161f233a 100644
--- a/vllm/entrypoints/openai/cli_args.py
+++ b/vllm/entrypoints/openai/cli_args.py
@@ -18,6 +18,7 @@ from vllm.entrypoints.openai.serving_models import (LoRAModulePath,
                                                     PromptAdapterPath)
 from vllm.entrypoints.openai.tool_parsers import ToolParserManager
 from vllm.logger import init_logger
+import vllm.envs as envs
 from vllm.utils import FlexibleArgumentParser
 
 logger = init_logger(__name__)
@@ -115,6 +116,12 @@ def make_arg_parser(parser: FlexibleArgumentParser) -> FlexibleArgumentParser:
                         default=None,
                         help="If provided, the server will require this key "
                         "to be presented in the header.")
+    parser.add_argument(
+        "--log-config-file",
+        type=str,
+        default=envs.VLLM_LOGGING_CONFIG_PATH,
+        help="Path to logging config JSON file for both vllm and uvicorn",
+    )
     parser.add_argument(
         "--lora-modules",
         type=optional_type(str),
diff --git a/vllm/transformers_utils/config.py b/vllm/transformers_utils/config.py
index 22ad9de73..e67239b29 100644
--- a/vllm/transformers_utils/config.py
+++ b/vllm/transformers_utils/config.py
@@ -307,6 +307,7 @@ def get_config(
                 "   Loading GGUF from a remote repo directly is not yet "
                 "supported.\n").format(model=model)
 
+            logger.exception(error_message)
             raise ValueError(error_message) from e
 
     if config_format == ConfigFormat.HF:
